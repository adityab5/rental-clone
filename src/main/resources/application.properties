spring.application.name=rental-local
spring.profiles.active=dev


# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/rental_db
spring.datasource.username=root
spring.datasource.password=pranam@2151

# Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Razorpay configuration (set via environment variables or local overrides)
razorpay.key-id=${RAZORPAY_KEY_ID:rzp_test_SVZgZiXtk4jDnZ}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:MSEzdHKzUVaOyS5HrbZzar2E}
razorpay.currency=${RAZORPAY_CURRENCY:INR}

# Gmail SMTP configuration (App Password required for production/dev use)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:bhushanadiit55555@gmail.com}
spring.mail.password=${MAIL_PASSWORD:utzxovylpajrlgtq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Notification settings
notification.mail.enabled=${NOTIFICATION_MAIL_ENABLED:true}
notification.mail.from=${NOTIFICATION_MAIL_FROM:noreply@example.com}
